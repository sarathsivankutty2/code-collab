# My Fullstack GraphQL Project

This is a sample monorepo that contains:
- A **React** frontend (`frontend/` folder)
- A **Node/TypeScript** GraphQL backend (`backend/` folder)
- (Optional) Dockerfile for ECS, Serverless config for Lambda

## Quick Start (Local)

1. **Install** dependencies:
   ```bash
   cd frontend
   npm install
   cd ../backend
   npm install
